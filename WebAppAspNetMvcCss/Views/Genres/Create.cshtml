@using WebAppAspNetMvcCss.Models
@model Genre

<h3>Жанр</h3>

@using (Html.BeginForm("Create", "Genres", FormMethod.Post))
{
    @Html.EditorForModel()

    <input type="submit" value="Сохранить" class="btn btn-primary" />
    @Html.ActionLink("Отменить", "Index", "Genres", new { @class = "btn btn-danger" })
}
